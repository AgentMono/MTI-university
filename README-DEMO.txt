MTI Student Portal — Fictional UI Demo

Open index.html.
Any email address logs in as Dummy. No password is used and no credentials are sent anywhere.
The package uses the supplied website assets/CSS as the visual base. The Student Dashboard task list is reproduced from the supplied visual reference.
The demo banner is intentional so this cannot be mistaken for the official portal.
